apiVersion: v1
kind: Service
metadata:
  name: {{ include "sre-portal.fullname" . }}
  labels:
    {{- include "sre-portal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "sre-portal.selectorLabels" . | nindent 4 }}
